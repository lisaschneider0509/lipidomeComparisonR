% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lipidome_comparison_clustering.R
\name{hclust_performance_table}
\alias{hclust_performance_table}
\title{Hierarchical clustering performance table}
\usage{
hclust_performance_table(
  input_df,
  dist_methods = c("euclidean", "maximum", "manhattan", "canberra", "minkowski"),
  hclust_methods = c("average", "single", "complete"),
  out_path = "none"
)
}
\arguments{
\item{input_df}{data frame.}

\item{dist_methods}{vector. Options: c("euclidean", "maximum", "manhattan", "canberra", "binary", "minkowski"). Default: c("euclidean", "maximum", "manhattan", "canberra", "minkowski")}

\item{hclust_methods}{vector. Options: c("ward.D", "ward.D2", "single", "complete", "average", "mcquitty", "median", "centroid"). Default: c("average", "single", "complete")}

\item{out_path}{string. Path to save the text output to. If not specified, the output is only printed to the device or saved to a variable. If specified, the output is additionally saved in a .txt-file.}
}
\description{
`hclust_performance_table` takes a data frame and calculates which distance function and hclust function combination has the best performance.
}
\details{
This function calculated the performance for all combinations of the following distance and hclust functions distance functions = c("euclidean", "manhattan"), hclust functions = c("average", "single", "complete")). The results are printed in a table. The combination with the highest performance value will produce the best clustering.
}
